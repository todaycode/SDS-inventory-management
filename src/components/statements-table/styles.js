import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  tableHeading: {
    fontSize: '18px',
    color: '#00324E',
    fontWeight: 'bold',
  },
});

export default useStyles;
